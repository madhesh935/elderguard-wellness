{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "NITRO_PRESET=static npm run build",
  "outputDirectory": ".output/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
